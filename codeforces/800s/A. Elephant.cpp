// https://codeforces.com/problemset/problem/617/A
// DISCLAIMER: This is NOT production-quality code.
#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);

    int x;
    cin >> x;

    cout << (x + 4) / 5; // (x + 4) / 5 is the same as ceil(x / 5.0)
}